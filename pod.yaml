apiVersion: v1
kind: Pod

metadata:
  name: dbpod
  # namespace: test
  labels:
    app: myapp1

spec:
  containers:
    - name: bala
      image: nginx
      env:
        - name: myName
          value: gaurav
        - name: myCity
          value: pune
      ports:
        - containerPort: 80