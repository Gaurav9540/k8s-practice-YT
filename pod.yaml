apiVersion: v1
kind: Pod

metadata:
  name: testnspod3
  # namespace: test
  labels:
    app: myapp

spec:
  containers:
    - name: bala
      image: nginx
      resources:
        requests:
          memory: 250Mi
          cpu: 100m
        limits:
          memory: 500Mi
          cpu: 500m
      env:
        - name: myName
          value: gaurav
        - name: myCity
          value: pune
      ports:
        - containerPort: 80