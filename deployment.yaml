apiVersion: apps/v1
kind: Deployment

metadata:
  name: firstdeploy
  labels:
    name: firstdeploy
spec:
  replicas: 10
  minReadySeconds: 30
  strategy:
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
    type: RollingUpdate
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      name: dpod
      labels:
        type: app
        app: myapp
    spec:
      containers:
        - name: container
          image: nginx
          ports:
            - containerPort: 80